services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
      - DB_PATH=/app/data/bot.sqlite3
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
