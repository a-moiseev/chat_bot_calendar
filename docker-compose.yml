services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
      - DB_PATH=/app/data/bot.sqlite3
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
    ports:
      # liveness probe наружу — для внешнего мониторинга (Uptime Kuma и т.п.)
      - "${HEALTH_PORT:-8080}:8080"
    healthcheck:
      # в slim-образе нет curl, поэтому проверяем через python
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/healthz', timeout=5)
      interval: 60s
      timeout: 10s
      start_period: 20s
      retries: 3
